 <div class="contents row" >
    <% @tweets.each do |tweet| %>
    <!-- eachは配列の要素一つ一つを取り出し処理をするメソッド。
    上記の場合だと@tweets配列を取り出しその要素をtweetという変数として扱う。 -->
      <%= render partial: "tweet",locals: {tweet: tweet} %>
      <!-- renderは部分テンプレートを呼び出すときに使うヘルパーメソッド。
      partialで具体的にどの部分テンプレートを呼び出すか指定できる。
      localsでは部分テンプレートに書いてある変数がhtmlのどの変数に対応しているのかを記述することができる。
      ↑の場合だと「_tweet.html.erbを指定してそのファイルのtweetはhtmlのtweet(eachによって呼び出された)として扱う」という意味になる。-->
    <% end %>
    <%= paginate(@tweets) %>
     <!-- paginateはページネーションを表現するために使うヘルパーメソッド。
    kaminariを導入することで使うことができる。
    引数にはpage,perメソッドの記述された変数が必要。 -->
  </div>